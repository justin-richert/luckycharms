language: python
python:
  - "3.6"
env:
  - PYTHONPATH=`pwd`
install:
  - pip install -r test_requirements.txt
script:
  - ./linters.sh && pytest tests/ --cov=luckycharms/ --cov-report term-missing
after_success:
  - coveralls
